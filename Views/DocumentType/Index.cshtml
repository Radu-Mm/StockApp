@model IEnumerable<StockApp.Models.DocumentTypeModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Nomenclator Tipuri Documente</h1>


<table class="table">
    <thead>
        <tr>
          @*  <th>
                @Html.DisplayNameFor(model => model.DocTypeId)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model.DocType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
        @*    <td>
                @Html.DisplayFor(modelItem => item.DocTypeId)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.DocType)
            </td>
                <td align="right">
                    @Html.ActionLink("Modificare", "Edit", new {  id=item.DocTypeId  }, new{ @class="btn btn-primary"}) 
               @*     @Html.ActionLink("Details", "Details", new {  id=item.DocTypeId  }) |*@
                    @Html.ActionLink("Stergere", "Delete", new {  id=item.DocTypeId  }, new{ @class="btn btn-primary"})
            </td>
        </tr>
}
    </tbody>
</table>
<h3></h3>
<p>
    <a asp-action="Create" class="btn btn-primary">Adaugare</a>
</p>